- name: Ansible playbook for Server
  hosts: localhost
  vars_files:
    - vars.yml
  roles:
    - git
    - tmux
    - k3s
    - helm
    # Don't change the order below this
    - zsh
    - fzf
    - starship
    - kubectl
