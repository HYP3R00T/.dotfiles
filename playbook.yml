- name: Ansible playbook
  hosts: localhost
  vars_files:
    - vars.yml
  roles:
    - git
    - tmux
    - fzf
    # Don't change the order below this
    - zsh
    - starship
    - uv
    - poetry
    - zoxide
    - kubectl
    - pnpm